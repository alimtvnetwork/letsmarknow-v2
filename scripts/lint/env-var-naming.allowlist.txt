# env-var-naming allowlist
# Format:
#   <file>            — silence the file entirely
#   <file>:<TOKEN>    — silence one env var in one file
#
# The conversation log contains historical instructions and may quote
# legacy / hypothetical env names that are not part of the canonical catalog.
spec/21-app/00-conversation-log.md
