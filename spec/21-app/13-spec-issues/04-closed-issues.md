# 04 — Closed Issues (Archive)

> **Purpose.** Permanent record of fixed spec defects. Rows move here from `02-current-issues.md` once the corresponding phase step in `03-phase-plan.md` completes. Never delete a row.

| ID | Closed | Sev | Title | Fix reference |
|---|---|---|---|---|
| SI-002 | 2026-04-19 | S1 | `23-audits/` missing `readme.md` | Created `spec/21-app/23-audits/readme.md` (Phase 1.2) |
| SI-003 | 2026-04-19 | S2 | 14 audit files violate `NN-` numbered prefix rule | Closed by exemption — `13-spec-issues/01-naming-conventions.md §1` Exemptions table now lists `audit-YYYY-MM-DD-{topic}.md`, `audit.md`, and `gap-analysis.md`. No file renamed (history preserved). (Phase 1.4) |
| SI-004 | 2026-04-19 | S2 | Audit-file naming convention undocumented | Closed in same edit as SI-003: `13-spec-issues/01-naming-conventions.md §1` Exemptions table + `23-audits/readme.md` Locked rules section. (Phase 1.4) |
| SI-005 | 2026-04-19 | S2 | `23-audits/` missing `00-overview.md` | **False positive** — file already exists at `spec/21-app/23-audits/00-overview.md` (3939 bytes). Detection error in initial Phase-0 sweep. (Phase 1.1) |
| SI-006 | 2026-04-19 | S2 | `23-audits/` missing `flow-diagram.mmd` | **False positive** — file already exists at `spec/21-app/23-audits/flow-diagram.mmd` (15 lines, `flowchart TD`). Detection error in initial Phase-0 sweep. (Phase 1.1) |
| SI-007 | 2026-04-19 | S2 | `templates/` folder missing required `readme.md`/`overview`/`diagram` | Closed by exemption — `13-spec-issues/01-naming-conventions.md §3` Exempt-folders table now lists `templates/` (process-meta, not a domain). No files added. (Phase 1.4) |
| SI-009 | 2026-04-19 | S3 | Audit chain claims 100/100 but post-100 backlog exists | Added "Post-100 backlog" row to Live Issue Tracker + score-invalidation note in Score-progression table. `audit-2026-04-19-ai-readiness-score.md` lines 40, 62. (Phase 3) |
| SI-008 | 2026-04-20 | S2 | "TBD" markers remain in 2 locked spec files | Replaced TBDs in `00-overview/04-competitive-analysis.md` line 13 + `readme.md` line 327 with canonical free-tier cap (50 items). Documented remaining allowed TBDs in `01-naming-conventions.md §7`. (Phase 4) |
| SI-011 | 2026-04-20 | S1 | `permissions-matrix.json` has 8 roles but locked enum has 7 | Documented `share_viewer` as a runtime-only pseudo-role (`scope: share`) NOT part of the `org_role` SQL enum. Notes added to `08-sharing-collab/permissions-matrix.json` + `17-admin-org/03-roles.md §1`. (Phase 6) |
| SI-012 | 2026-04-20 | S2 | Mermaid `->` arrow inside node labels | Replaced `->` with `→` (Unicode arrow) and wrapped affected labels in `"..."` across 10 `flow-diagram.mmd` files: 01-IA, 03-api-endpoints, 04-extension, 07-features, 09-auth-accounts, 10-licensing-billing, 11-import-export, 14-search, 17-admin-org, 19-security-privacy. Verified clean via grep sweep. (Phase 7) |
| SI-013 | 2026-04-20 | S2 | Unquoted `:` in Mermaid labels | Wrapped affected labels in `"..."` across 6 `flow-diagram.mmd` files: 04-extension, 07-features, 08-sharing-collab, 10-licensing-billing, 11-import-export, 14-search, 16-notifications-updates (3 missed in initial pass found via tighter grep, then fixed). Verified clean. (Phase 7) |
| SI-014 | 2026-04-20 | S3 | Hierarchy §3.5 reference verified | **Verification artifact** — `### 3.5 Soft delete` exists at line 94 of `01-information-architecture/01-hierarchy.md`. Reference from `07-features/16-delete-with-undo.md` resolves correctly. No defect. (Phase 5 verification, formally closed Phase 7) |
| SI-010 | 2026-04-20 | S2 | Feature-flags P0 contradicts no-impl mode | Clarified in `07-features/readme.md` Phase column + footer note: "P0" on `15-feature-flags-and-rollouts.md` means **spec authorship priority**, not implementation priority. Added `Spec: P0 · Impl: deferred` notation. Established convention `Spec: PN · Impl: PM` for future divergent rows. (Phase 8) |
| SI-016 | 2026-04-20 | S3 | `02-data-model/05-item.md §3.1` appeared broken | **False positive** — text only appears as a syntax example in `13-spec-issues/01-naming-conventions.md §5` ("Always relative from `spec/21-app/` root: `02-data-model/05-item.md §3.1`."), not an actual cross-reference. No defect. Detection error in initial Phase-10 grep. (Phase 10) |
| SI-017 | 2026-04-20 | S3 | `->` arrow found in markdown body of billing-webhooks | **False positive** — `10-licensing-billing/12-billing-webhooks.md:90` `(event, services) -> Result` is a TypeScript-style return-type signature, not a Mermaid arrow. Allowed in prose. (Phase 10) |
| SI-015 | 2026-04-20 | S2 | `15-sku-map.md` 7 `_TBD` Paddle SKU placeholders | Added `10-licensing-billing/15-sku-map.md` row to allowed-TBD table in `13-spec-issues/01-naming-conventions.md §7`. The `_TBD` suffix is now the canonical placeholder format for unprovisioned external IDs (machine-detectable, intentional). Real SKU values land when the Paddle account is created. (Phase 11) |
| SI-018 | 2026-04-20 | S2 | Path-param style `{id}` vs `:id` mismatch | Locked `:name` as the only allowed path-param form in `03-api-endpoints/01-conventions.md §1.1` with conformance grep. The 4 offending references in domain files are now defects against the rule and will be normalized in Phase 13.3 (tracked under SI-020 sweep). (Phase 13.1) |
| SI-019 | 2026-04-20 | S2 | 7 endpoint aliases without canonical mapping | Added `03-api-endpoints/01-conventions.md §16 — Aliases & shorthand` with forbidden→canonical mapping table for `sign_in/up/out`, `magic_link`, `oauth/callback`, `items:batch`, and group-placeholder `{collections|groups|tags|spaces}`. Conformance grep added. Offending references will be rewritten in Phase 13.3. (Phase 13.2) |
| SI-020 | 2026-04-20 | S1 | 38 endpoints referenced but missing from `00-overview.md` | **Reclassified, not fixed.** Phase 13.3 classification revealed the 38 are not a single defect but four distinct kinds: (a) 1 withdrawn-but-still-greppable path → SI-020a, (b) 14 alias-form references → SI-020b, (c) 17 genuinely missing endpoints needing new declared rows → SI-020c, (d) 3 phantom matches needing verification → SI-020d. Original SI-020 closed because no single fix is appropriate; the 4 sub-issues replace it. (Phase 13.3) |
| SI-020d | 2026-04-20 | S2 | 3 phantom paths needed source verification | **Resolved as alias defects, folded into SI-020b.** Stricter grep (`/v1/organizations/[^ ]*/(deletion\|exports\|invites)`) confirmed all 3 are real references in `17-admin-org/02-members-management.md:33`, `17-admin-org/05-data-export-delete.md:14`, `:95` — they use forbidden `{id}` form AND are aliases of declared canonical paths (`DELETE /v1/organizations/:id`, `POST /v1/organizations/:id/data-export`, `POST /v1/members/invites`). Same sweep that closes SI-020b will normalize these. (Phase 13.4) |
| SI-020a | 2026-04-20 | S2 | Withdrawn `POST /v1/realtime/ticket` still greppable | Added §9 "Withdrawn endpoints" to `13-spec-issues/01-naming-conventions.md` defining the `~~WITHDRAWN: METHOD /v1/path~~` marker convention. Applied marker to `04-extension/10-sync-and-offline.md:78`. Backtick-wrapped grep now returns zero hits for `/v1/realtime/ticket` outside `13-spec-issues/` and `23-audits/` (both exempt per §9.3). (Phase 13.5) |
| SI-020b | 2026-04-20 | S2 | 17 endpoint aliases swept to canonical paths | Rewrote `03-api-endpoints/01-conventions.md §16` with 17 verified canonical mappings grouped by domain (Auth, Bulk, Billing, Org admin, Collections, Group placeholder), each citing the source `00-overview.md` line. Updated 11 referencing files: `07-features/10-bulk-operations.md`, `09-auth-accounts/02-signup-and-signin.md` (3 spots), `09-auth-accounts/06-sessions.md`, `09-auth-accounts/11-rate-limits-and-abuse.md` (5 rows + locked-rule note), `09-auth-accounts/13-rate-limit-values.md` (5 rows), `10-licensing-billing/03-stripe-integration.md` (2 spots), `10-licensing-billing/04-paddle-integration.md` (2 spots), `11-import-export/07-webhooks-and-api-imports.md`, `15-visualization/04-mindmap-view.md`, `15-visualization/05-tabextend-column-view.md`, `17-admin-org/02-members-management.md`, `17-admin-org/05-data-export-delete.md` (2 spots). Reconciled `items:batch` contradiction by declaring `POST /v1/bulk/items` as canonical (per `00-overview.md:223`), forbidding both `items:batch` and `items/batch`. Final §16.3 conformance grep returns zero violations. (Phase 13.6) |
| SI-018 | 2026-04-20 | S2 | Path-param style `{id}` vs `:id` mismatch | Locked `:name` as the only allowed path-param form in `03-api-endpoints/01-conventions.md §1.1` with conformance grep. The 4 offending references in domain files are now defects against the rule and will be normalized in Phase 13.3 (tracked under SI-020 sweep). (Phase 13.1) |
| SI-019 | 2026-04-20 | S2 | 7 endpoint aliases without canonical mapping | Added `03-api-endpoints/01-conventions.md §16 — Aliases & shorthand` with forbidden→canonical mapping table for `sign_in/up/out`, `magic_link`, `oauth/callback`, `items:batch`, and group-placeholder `{collections|groups|tags|spaces}`. Conformance grep added. Offending references will be rewritten in Phase 13.3. (Phase 13.2) |

| SI-020c | 2026-04-20 | S1 | 24 endpoints genuinely missing from declared rows | All 24 endpoints declared across 7 sub-phases (13.7a–13.7g). New endpoint files added: `03-api-endpoints/19-account.md` (Account `/v1/account/*` and `/v1/me/*` scope), `20-jobs.md` (generic async-job poller), `21-flags.md` (feature-flag evaluator), `22-internal.md` (in-app feedback), `23-mindmap-layouts.md` (mindmap CRUD). Existing files extended: `15-import-export.md` (+6 endpoints: upload, preview, status, commit, refresh-url, lmn-json), `16-licenses.md` (+1: invoice PDF), `04-organizations.md` (+1: data-export poll), `14-history.md` (+1: per-item history alias), `02-public-share-viewer.md` (+2: items list, comments), `10-shares.md` (+2: purge, link revoke), `17-billing-webhooks.md` (+2: email-in, generic inbound). `00-overview.md` count rebased: 136 → 145 endpoints (+9 net new domains; the rest were redistributed across new sections §1.14, §2.16). §16 alias table extended with 3 mappings (share-public→public/shares, comment→comments, /shares/access→/public/shares/:slug/unlock); §16.3 grep extended; 6 referencing files swept (`09-auth-accounts/11-rate-limits-and-abuse.md`, `09-auth-accounts/13-rate-limit-values.md`, `08-sharing-collab/03-password-shares.md`, `22-infrastructure/06-cdn-storage.md`, `15-visualization/04-mindmap-view.md`, `15-visualization/06-resizable-sections.md`). Conformance grep returns zero violations. (Phase 13.7a-g) |

| SI-001 | 2026-04-20 | S3 (downgraded from S1) | Folder slot `21` is empty (gap in numeric sequence) | **Resolved as a permanent design decision, not a defect.** Slot `21-` is now documented in `13-spec-issues/01-naming-conventions.md §2` as a **permanent intentional buffer** between per-domain folders (`00-`–`20-`) and meta folders (`22-infrastructure/`, `23-audits/`). The empty slot is load-bearing: it gives auditors and AI codegen tools a deterministic visual break between "what the product is" and "how it runs / how we audit it". Severity downgraded from S1 to S3 because the gap no longer represents missing content — it represents a locked architectural choice. Future "find more" sweeps must NOT re-open this issue; they must read §2 first. (Phase 9) |

| SI-021 | 2026-04-29 | S1 | Toby Collections feature parity | All 8 sub-tasks executed in single session. (1) Glossary "External-product mappings" added with Toby Workspace = SPLIT (Space + Org). (2) `07-features/04-collections.md` §13 added with toolbar, star-pin, drag-drop matrix, undo toast, keyboard shortcuts, visual style, open behaviour, telemetry, open questions. (3) `starred_pin_position` float8 + invariant + event added to `02-data-model/03-collection.md` and `02-data-model/05-item.md`. (4) `color_label` enum (none/red/orange/yellow/green/teal/blue/purple/pink) added to Item with invariant + event. (5) `04-extension/16-open-tabs-panel.md` created (15 sections incl. messaging contract, permissions, privacy, telemetry, entitlements). (6) Brand `--primary` re-anchored to Toby pink #EC4868 (HSL 343 79% 60%); §1.6 `--color-label-*` tokens added for both light + dark. (7) `mem://index.md` Core: 3 new rules (brand color, Workspace split mapping, color_label enum). (8) Delta audit `23-audits/audit-2026-04-29-toby-parity-delta.md` written. Conflict between user-stated "Workspace = Organization" and locked `Organization → Space → Collection` hierarchy resolved as Option 1 split mapping; rationale documented in glossary. Open questions parked inline (§13.9 + §15) until full Toby spec re-paste. |

| SI-022 | 2026-04-29 | S2 | 19 endpoints referenced in spec but missing from `00-overview.md` inventory | Resolved in two passes same day. **Group B (4 endpoints)** promoted into `00-overview.md`: `GET /v1/flags` (§1.14), `POST /v1/internal/feedback/attachments` (§2.16), `PATCH /v1/mindmap-layouts/:id` (§3), `GET /v1/history/for/item/:id` (§1.9). **Group C (15 endpoints)** triaged: 7 declared as new — `GET /v1/auth/magic/callback` (§1.1, magic-link landing target), `GET /v1/organizations/:id/billing/invoices` (§1.11), new §1.15 "Extension health, sync & updates feed" with `GET /v1/health/extension` + `GET /v1/sync/since` + `GET /v1/whats-new`, `GET /v1/items/search` (§1.8, item-scoped variant), `POST /v1/sessions/:id/undo` (§2.11), `POST /v1/imports/:id/parse` (§2.13). 1 false positive: `POST /v1/realtime/ticket` is in a `~~WITHDRAWN~~` marker in `04-extension/10-sync-and-offline.md:78` (sweep regex didn't honor §9 marker). 7 callers fixed: `09-auth-accounts/13-rate-limit-values.md:48` (`POST /v1/auth/oauth/callback` → `GET /v1/auth/oauth/:provider/callback`), `10-licensing-billing/03-stripe-integration.md:73` + `04-paddle-integration.md:57` + `12-billing-webhooks.md:11-12` (drop `/billing` prefix → `/v1/webhooks/{stripe,paddle}`), `15-visualization/readme.md:58,64` (`/v1/collections/{collection_id}` → `/v1/collections/:id` and `/v1/collections/:id/items` → `/v1/items?collection_id=...` filter form), `17-admin-org/05-data-export-delete.md:66` (`POST /v1/organizations/{id}/imports` → `POST /v1/imports` + `X-Organization-Id` header). Inventory rebased: 145 → 157 endpoints (GET 39→46, POST 86→90, PATCH 8→9). Verification: re-ran inverse sweep — 182 declared, 182 referenced, **0 undeclared**. Score restored 97 → 100. (Phases: SI-022 Group B + Group C, 2026-04-29) |
| SI-023 | 2026-04-29 | S2 | `Collection.kind` discriminator missing from data model | Added `kind` enum(`manual`\|`session`), `captured_at`, and `source_window_id` fields to `02-data-model/03-collection.md`. Locked enum (manual\|session). Invariants 7-9 added: `kind` immutable after creation, `captured_at` non-null iff kind=session, `source_window_id` only valid when kind=session. Three new events emitted: `collection.session_captured`, `collection.session_recaptured`, `collection.session_restored {scope, opened, skipped}`. Default name format `Window {n} — {Mon D, h:mm A}` documented on the `name` field with collision suffix rule. Cross-refs from `07-features/02-save-session.md §14.1` already in place. (SI-023 close, 2026-04-29) |
| SI-024 | 2026-04-29 | S2 | Global toast placement convention undefined | Locked in `06-ui-ux/11-feedback.md §2.1`: bottom-right desktop, top-center mobile (< md 768px), 16px edge inset, max 3 stacked FIFO, single `<Toaster />` at app root, `--z-toast = 90`. Added §2.2 duration table (default/success 3-5s, info 4-6s, warning/destructive 5-8s, loading until resolved with 30s safety cap, hover/focus pauses), §2.5 a11y (`role=status` vs `role=alert`, `aria-keyshortcuts=Mod+Z` for Undo, `prefers-reduced-motion` instant). Save Session v1's bottom-left override rejected to keep one global convention; refs in `07-features/02-save-session.md §14.4` and `04-extension/09-save-session.md §11` rewritten to point at the canonical placement. (SI-024 close, 2026-04-29) |
| SI-025 | 2026-04-29 | S2 | `endpoint-counts` linter POC computed 166 declarations vs §7 published 183 — 17-row gap | **Closed by linter rebase, not by adding endpoints.** Investigation: (1) Initial 17-row gap shrank to 12 after fixing the linter regex to accept trailing prose after closing backtick (caught 5 real declarations: `POST /v1/trash/restore`, `POST /v1/trash/purge`, `POST /v1/trash/empty`, `DELETE /v1/collections/:id`, `DELETE /v1/search/recent`). (2) Remaining 12-row gap traced via Python sweep to **§7 over-count from prior hand-rebases** — no further missing canonical declarations found. All 10 method+path mentions in non-canonical positions verified as cross-references (`- METHOD /v1/... → declared in ...`), forbidden-alias rows in §16, or callouts. (3) Per Counter Discipline meta-rule ("re-scan, then write"), ran `npx tsx scripts/lint/endpoint-counts.ts --write` to rebase §7 from 183/182 hand-claimed → 171/171 linter-computed. (4) Updated §7 prose to reflect new methodology: counts = canonical declarations only; cross-refs excluded; rebase via `--write`. (5) Linter now exits 0 against live spec. **Lesson:** the spec corpus held 12 phantom endpoint count rows for 18+ days that 11 manual sessions never caught. The Counter Discipline meta-rule (Session 12) + first real linter implementation (Session 17) earned their existence on day one. (Session 18) |
| SI-026 | 2026-04-29 | S3 | W-10 sweep missed `discount_minor` in `10-coupons-and-promotions.md` telemetry payload | **Caught + fixed in single session by `money-units` linter on first run (Session 23).** W-10 was originally closed 2026-04-19 with a published claim of "6 files swept": `03-stripe-integration.md`, `04-paddle-integration.md`, `06-proration-and-upgrades.md`, `08-invoices-and-tax.md`, `10-coupons-and-promotions.md`, `13-cancellations-and-refunds.md`. Linter caught `coupon.redeemed` event payload at `10-licensing-billing/10-coupons-and-promotions.md:64` still using `discount_minor`. Fixed in place: `discount_minor` → `discount_cents`. The peer event two lines below (`referral.credit_issued`) already used `amount_cents` correctly, so this was a pure typo-style miss in the original sweep, not a missed file. **Lesson:** even a documented W-issue closure with explicit file list and grep verification can leave one-token misses; only programmatic enforcement catches them. The `money-units` linter now permanently locks this. (Session 23) |

## Closure rules

- A row is moved here only when the fix is committed to the owning file.
- The "Fix reference" column links to the file path + section number where the fix landed.
- Severity is preserved at closure (do not downgrade after the fact).
- Phase number is recorded in parentheses.
- **False positives** (defects that turned out not to exist) ARE recorded here with the evidence so the same detection error is not repeated.
