# 25-references/

External-product reference docs the user has shared for parity / inspiration. **Not authored spec.** Files here are read-only context — the canonical spec stays under `00-`–`24-`. When a reference doc surfaces a gap, fix the gap in the proper domain folder and log the audit under `23-audits/`.

## Index

| File | Source | Captured | Used by audit |
|---|---|---|---|
| `toby-invite-share-v1.md` | Toby (gettoby.com) — Invite & Share feature spec v1.0 | 2026-04-30 (Session 117) | `audit-2026-04-30-toby-invite-share-parity-117.md` |

## Rules

- Never edit a reference doc to "fix" it — it is a snapshot of someone else's product.
- Never link a reference doc from runtime spec files (only audits may link here).
- When adding a new reference, append a row above and write a parity audit in `23-audits/` the same session.
